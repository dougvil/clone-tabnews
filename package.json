{
  "name": "clone-tabnews",
  "version": "1.0.0",
  "description": "Implementação tabnews.com.br seguindo curso.dev",
  "main": "index.js",
  "scripts": {
    "dev": "npm run services:up && npm run wait-for-db && npm run migration:up && next dev",
    "services:up": "docker compose -f infra/compose.yaml up -d",
    "services:down": "docker compose -f infra/compose.yaml down",
    "services:stop": "docker compose -f infra/compose.yaml stop",
    "services:logs": "docker compose -f infra/compose.yaml logs -f",
    "lint:prettier:check": "prettier --check .",
    "lint:prettier:fix": "prettier --write .",
    "lint:eslint:check": "next lint --dir .",
    "test": "npm run services:up && npm run wait-for-db && concurrently -n next,jest --hide next -k -s command-jest \"next dev\" \"jest --runInBand\"",
    "test:watch": "jest --watchAll --runInBand",
    "migration:create": "node-pg-migrate create -m infra/migrations",
    "migration:up": "node-pg-migrate -m infra/migrations --envPath .env.development up",
    "migration:down": "node-pg-migrate -m infra/migrations --envPath .env.development down",
    "wait-for-db": "node infra/scripts/wait-for-postgres.js",
    "prepare": "husky"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "async-retry": "1.3.3",
    "dotenv": "16.6.1",
    "dotenv-expand": "11.0.7",
    "next": "^14.2.5",
    "pg": "8.18.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@commitlint/cli": "19.8.1",
    "@commitlint/config-conventional": "19.8.1",
    "concurrently": "8.2.2",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.4",
    "eslint-config-prettier": "9.1.2",
    "eslint-plugin-jest": "^28.14.0",
    "husky": "9.1.7",
    "jest": "29.7.0",
    "node-pg-migrate": "^7.6.1",
    "prettier": "3.8.1"
  }
}
